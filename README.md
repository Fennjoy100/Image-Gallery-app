Image Gallery (HTML & CSS only)

Files:

- `index.html` — main static page
- `styles.css` — all styles

How to use:

1. Open `index.html` in your browser (double-click or right-click -> Open with).
2. The page is pure HTML/CSS and uses remote images from Unsplash.

Want changes?
- I can update spacing, colors, or swap images to local files if you prefer.
